# Stage 3 Next Step Recommendation

Proceed to Stage 4 limited baseline reproduction.

Recommended Stage 4 scope:

- Keep the run bounded, such as 3 LIBERO Goal tasks with 5 or 10 episodes each.
- Preserve Stage 3 separation between top-level model env and `.venv-libero` client env.
- Keep videos, checkpoints, converted weights, datasets, and profiler binaries local-only and gitignored.
- Report success rate and latency as preliminary limited-baseline evidence, not paper reproduction.
