# Next Step Recommendation

Resolve the base checkpoint path.

Stage 2 installed the top-level model environment, applied the transformers patch, downloaded and loaded the `libero_goal` draft checkpoint, and converted the draft checkpoint for Triton. The blocker is the unresolved official FLASH base checkpoint path for `pi0_libero` base Triton conversion.

Do not proceed to Stage 3 one-task closed-loop smoke until base conversion succeeds. Do not run a full benchmark or claim paper reproduction.
