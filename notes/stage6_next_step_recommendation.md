# Stage 6 Next Step Recommendation

Proceed to Stage 5 camera dropout robustness probe.

Recommended next scope:

- Use the same bounded task subset before expanding any evaluation.
- Keep the Stage 6 timing patch available for later profiling comparisons.
- Add a per-route warm-up before making stronger p99 latency statements.
- Continue to keep videos, checkpoints, converted weights, datasets, and profiler binaries local-only and gitignored.
